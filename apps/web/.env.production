# Committed on purpose (production only): the build fetches all models
# into public/models, so production serves them same-origin — required for
# background removal, and it makes the offline PWA fully self-contained.
NEXT_PUBLIC_MEDIAPIPE_BASE=/models/wasm
NEXT_PUBLIC_MEDIAPIPE_MODEL=/models/face_landmarker.task
